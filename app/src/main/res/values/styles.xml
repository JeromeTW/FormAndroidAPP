<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="checkBoxText" parent="@android:style/TextAppearance">
        <item name="android:textSize">20dp</item>
    </style>
    <style name="radioButtonText" parent="@android:style/TextAppearance">
        <item name="android:textSize">20dp</item>
    </style>
    <style name="sectionText" parent="@android:style/TextAppearance">
        <item name="android:textSize">45dp</item>
    </style>
    <style name="questionTitleText" parent="@android:style/TextAppearance">
        <item name="android:textSize">34dp</item>
    </style>
    <style name="textPlainText" parent="@android:style/TextAppearance">
        <item name="android:textSize">20dp</item>
    </style>
</resources>
